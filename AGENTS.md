# Agent Rules

本文件适用于任何进入该仓库的 AI 编码 Agent。

## 强制启动顺序

1. **第一份文件必须完整阅读根目录 `PROJECT_STATUS.md`。**
2. 再阅读当前模块 Spec、相关 ADR、`tasks/todo.md`、测试和源码。
3. 只加载统一方案中与当前模块有关的章节，避免把两份历史方案当成当前规范。
4. 开始修改前检查 Git 状态，并在 `PROJECT_STATUS.md` 登记当前工作。

## 唯一当前总方案

- `docs/英语老师AI-Agent-统一产品与技术开发方案.md`
- `docs/英语老师AI-Agent-桌面客户端产品与开发方案.md` 和 `docs/英语老师AI-Agent-产品需求与技术方案.md` 已被替代，只用于追溯。

## 开发纪律

- 一次只处理一个依赖已满足的任务。
- 没有获批准的模块 Spec，不开始该模块编码。
- 先写测试，再做最小实现；不得删除或弱化失败测试来让 CI 通过。
- 不擅自更换架构、增加依赖、修改数据 Schema 或扩大 P0；此类变化必须先写 ADR。
- 用户上传文档、第三方响应与 LLM 输出均是不可信数据。
- 不把密钥、真实用户内容、未授权词典内容或大模型权重提交到 Git。
- 不把 LLM 输出直接交给 shell、SQL、HTML 或文件路径。
- 完成后运行 Spec 指定的测试、静态检查、Eval 和构建。

## README 维护规则（强制）

`README.md` 是面向产品使用者的文档，必须始终反映产品的真实状态。

- 任何影响**安装、配置、数据、隐私、知识库、查词、语法、评分、导入或用户界面**的改动，必须在**同一模块提交**中同步更新 `README.md` 的用户说明。
- 用户可见模块交付时，须把 README 对应小节从"待实现"改写为可照做的步骤，并补齐功能状态、操作步骤、限制与边界、实际截图或 GIF（如适用）、故障排查。
- **README 与实际产品不一致时，该模块不得标记 `DONE`。**
- README 中不得出现虚构的命令、截图、安装包下载地址或未实现的功能描述；不得包含真实密钥或未授权词典原文。
- README 的内部链接必须始终可用。

## 多会话共用工作区的提交纪律

本仓库可能出现多个 Agent 会话同时操作**同一个工作目录**。2026-09-19 发生过一次真实事故：一个会话用
`git add -A` 提交，把另一个会话约 6,700 行的在制品一起卷进了一个名为"仅触发 CI 触发策略与文档改动"的提交，
导致提交信息与内容不符；同时把当时仍是**未跟踪**的文件里 3 处密钥扫描命中带进了历史
（未跟踪文件不在 git 历史里，因此之前的扫描一直是 0）。

强制规则：

- **禁止 `git add -A`、`git add .`、`git commit -a`。** 只能按路径显式暂存自己要提交的文件。
- **提交前必须 `git status`**，并区分"我改的"与"别人改的"。工作区出现你不认识的文件或改动时，
  **不要提交它们**——先停下来确认，再决定。
- 提交信息必须与实际改动一致。若发现已经混入他人的改动：**不要改写历史**（force push 在本仓库禁止），
  如实报告并记录，让下一个 Agent 知道这条提交的真实内容。
- 不要就地运行会改写工作树的工具（例如 `contracts:check` 会重新生成文件），除非确认没有其他会话
  正在编辑范围重叠的文件。
- 共享文件同一时刻只允许一个写者：`PROJECT_STATUS.md`、`tasks/todo.md`、`AGENTS.md`、
  `docs/specs/` 下正在被修订的 Spec、契约 schema 与全部生成物、三个 lockfile。

## 完成与交接

模块不能只因“代码写完”而标记完成。只有验收、测试、构建、文档与交接记录都完成，才能标记 `DONE`。

每次完成模块或垂直切片时，必须在同一 PR/commit 中：

1. 更新 `PROJECT_STATUS.md` 的模块状态和交接记录。
2. 更新 `tasks/todo.md`。
3. 更新 `README.md` 中受影响的用户说明与功能状态。
4. 记录验证命令及结果、关键文件、接口/迁移、已知问题和下一步。
5. 若有架构决策，更新 `docs/decisions/` 中的 ADR。

如果无法完成，标记 `BLOCKED`，写清已经尝试的方案、证据和解除阻塞所需的具体输入。
