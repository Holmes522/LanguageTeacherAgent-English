# pnpm 配置（SPEC-M00 §6）
#
# 这里只放"让安装边界可复现"所必需的开关。任何新增配置都需要在 SPEC-M00 或 ADR 中说明理由。

# peer 依赖不满足时直接失败，而不是静默继续 —— 静默的 peer 漂移会让"可复现"失效。
strict-peer-dependencies=true

# 不隐式安装缺失的 peer，依赖必须显式声明在 package.json 中。
auto-install-peers=false

# engines.node 不满足时拒绝安装，让 Node 版本固定真正生效。
engine-strict=true
